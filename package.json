{
  "name": "github-upm-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker that proxies a GitHub Packages npm registry as a Unity UPM (scoped registry) endpoint, backfilling the legacy /-/all and /-/v1/search endpoints that GitHub does not implement.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.0",
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1",
    "wrangler": "^3.78.0"
  }
}
